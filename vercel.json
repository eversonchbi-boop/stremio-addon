{
    "rewrites": [
          { "source": "/manifest.json", "destination": "/api/manifest" },
          { "source": "/catalog/:type/:id", "destination": "/api/catalog/:type/:id" },
          { "source": "/meta/:type/:id", "destination": "/api/meta/:type/:id" },
          { "source": "/stream/:type/:id", "destination": "/api/stream/:type/:id" }
    ]
}
